In order to start developing you only need `npm start` and press button for your prefered device/emulator (`w` for web would be the simplest and easiest probably).

You can also try running it directly `npm start [web/Android/etc]`
